/*
Write a method to replace all spaces in a string with '%20'.
You may assume that the string has sufficient space at the end to store additional characters.
Also given the true length of the string. 
*/

String URLify (String s, int length) {
	if (length == 0) {
		return "";
	}

	char[] content = s.toCharArray();
	for (int i = 0; i < content.length; i++) {

	}

}